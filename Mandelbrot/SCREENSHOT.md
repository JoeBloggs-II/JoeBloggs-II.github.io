# Mandelbrot Screenshot

This screenshot is generated during testing because the PR tooling does not support committing binary files.

To regenerate:

1. Start a local static server from the repo root.
2. Open `Mandelbrot/index.html` in a browser.
3. Zoom in and capture a screenshot.
